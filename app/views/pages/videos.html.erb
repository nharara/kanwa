<div class="blue_wall container">
  <div class="row justify-content-center">
    <h1 class='text-center mb-3'>Some Food For Thought</h1>
    <% if @entry.present? %>
      <p class='p-3 text-center'>Since you recently felt <em><%= "#{@entry.emotion.name}" %></em> about your <b><%= "#{@entry.situation}" %></b>, here are some videos that might help:</p>
      <%@video_ids.each do |id|  %>
        <div class="col-12 p-4 my-2 shadow d-flex justify-content-center">
          <iframe
        class='video opacity'
        src="https://www.youtube.com/embed/<%=id%>"
        title="YouTube video player"
        frameborder="0"
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
        allowfullscreen></iframe>
        </div>
      <% end %>
    <% else %>
      <p class='p-3 text-center'> Make an entry first to see your list of recommended videos.</p>
    <% end %>
  </div>
</div>
